function main() {
    // Write your code here. Read input using 'readLine()' and print output using 'console.log()'.
    const PI = Math.PI;
    let r;
    r = Number(readLine());
    let area, per;
    area = PI * r * r;
    console.log(area);
    per = 2 * PI * r;
    console.log(per);
}
